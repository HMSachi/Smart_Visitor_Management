import React from 'react';
import { QrCode, RefreshCw, XCircle, Search, Calendar, ChevronRight } from 'lucide-react';

const QRCodeInfo = ({ name, id, validUntil, status }) => (
  <div className="p-4 border border-mas-border bg-mas-gray/20 hover:border-mas-red/50 transition-all group flex items-center justify-between">
    <div className="flex items-center gap-4">
      <div className="w-12 h-12 bg-white flex items-center justify-center border-2 border-mas-gray group-hover:border-mas-red transition-colors">
        <QrCode size={32} className="text-mas-black" />
      </div>
      <div>
        <p className="text-xs font-bold text-white uppercase tracking-tight">{name}</p>
        <p className="text-[9px] text-mas-text-dim uppercase tracking-widest mt-0.5">ID: {id}</p>
      </div>
    </div>
    
    <div className="flex items-center gap-8">
      <div className="text-right hidden sm:block">
        <p className="text-[9px] text-mas-text-dim uppercase tracking-[0.2em] font-bold">Valid Until</p>
        <div className="flex items-center gap-1 justify-end text-white">
          <Calendar size={10} className="text-mas-red" />
          <span className="text-[10px] font-medium">{validUntil}</span>
        </div>
      </div>
      
      <div className="flex gap-2">
        <button title="Regenerate" className="p-1.5 border border-mas-border text-mas-text-dim hover:text-white hover:border-mas-red transition-all">
          <RefreshCw size={14} />
        </button>
        <button title="Revoke" className="p-1.5 border border-mas-border text-mas-text-dim hover:text-mas-red hover:border-mas-red transition-all font-bold">
          <XCircle size={14} />
        </button>
      </div>
    </div>
  </div>
);

const QRCodeManagement = () => {
  const codes = [
    { name: 'External Audit Team (3)', id: 'QR-00892', validUntil: '25 Mar, 06:00 PM', status: 'active' },
    { name: 'Supplier: TechLogistics', id: 'QR-00914', validUntil: '24 Mar, 08:00 PM', status: 'active' },
    { name: 'Temporary Maintenance', id: 'QR-00915', validUntil: '24 Mar, 11:59 PM', status: 'active' },
  ];

  return (
    <div className="mas-panel h-full flex flex-col">
      <div className="p-6 border-b border-mas-border flex justify-between items-center bg-mas-dark/50">
        <div>
          <h2 className="text-sm font-bold uppercase tracking-[0.2em] text-white">QR Code Control</h2>
          <p className="text-[10px] text-mas-text-dim uppercase tracking-widest mt-1">Pre-approval & token management</p>
        </div>
        <button className="mas-button-primary flex items-center gap-2">
          <QrCode size={16} /> NEW VISITOR PASS
        </button>
      </div>

      <div className="p-4 bg-mas-gray/20 border-b border-mas-border flex gap-4">
        <div className="relative flex-1">
          <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-mas-text-dim" size={14} />
          <input 
            type="text" 
            placeholder="Search passes..." 
            className="w-full bg-mas-dark border border-mas-border py-1.5 pl-10 pr-4 text-xs text-white focus:border-mas-red outline-none uppercase placeholder:text-[9px] tracking-widest"
          />
        </div>
        <button className="px-3 border border-mas-border text-mas-text-dim hover:text-white transition-all">
           <Search size={14} />
        </button>
      </div>

      <div className="p-4 space-y-3 flex-1 overflow-y-auto">
        {codes.map((code, i) => (
          <QRCodeInfo key={i} {...code} />
        ))}
      </div>

      <div className="p-4 border-t border-mas-border bg-mas-dark/30">
        <button className="w-full py-2 flex items-center justify-between text-[10px] font-bold uppercase tracking-[0.2em] text-mas-text-dim hover:text-mas-red group transition-all">
          <span>View all active tokens</span>
          <ChevronRight size={14} className="group-hover:translate-x-1 transition-transform" />
        </button>
      </div>
    </div>
  );
};

export default QRCodeManagement;
